# ── EDA tool working directories ─────────────────────────────────────────────
joules_work/
fv/
xcelium.d/
*.db               # Joules / Genus checkpoint databases
*.jdb
*.proto.db
AESCpu.proto.db

# ── EDA log / report files (regenerated on every run) ────────────────────────
flownet.log
flownet_*.log
timing_report.log
timing_report22.log
sdf.log
xrun.log
xrun.history

# ── Generated trace data (large binary / text, regenerated by the pipeline) ──
generated_vmems_key1/
generated_vmems_key2/
vcds/
*.vcd                  # gate-level VCD files (tens to hundreds of MB each)
Result/*.data          # Joules time-series power traces
Result/*.vcd

# ── Intermediate build artefacts ─────────────────────────────────────────────
*.o
*.elf
*.bin

# ── OS / editor clutter ───────────────────────────────────────────────────────
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
